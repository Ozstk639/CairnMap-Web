# Card Legacy Compatibility

Card config is the definition source. Legacy card files remain as runtime executors for rows, interactions, trade sections, media sections, and fallback rendering.

Do not add new Class-level card layouts in legacy card registries. Add them in Class `card` sections and `shared/card/` registries.
