# Display shared config

Display profiles, label styles, runtime contracts, algorithms, and special display logic registry. Root-level files are retained temporarily as compatibility copies during CM_CONFIG_STRUCTURE_1.
